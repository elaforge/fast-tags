-- | Normally this module is shadowed by the cabal-generated one, but having
-- one here means ghci works.
module Paths_fast_tags where
import qualified Data.Version as Version


version :: Version.Version
version = Version.Version [0] ["not built with cabal"]
